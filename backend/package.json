{
  "name": "farmer-gpt-backend",
  "version": "1.0.0",
  "description": "Simple API for Farmer GPT (health + echo)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
